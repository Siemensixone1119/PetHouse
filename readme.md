## Запуск проекта локально

Для проверки работоспособности PHP-скриптов необходимо запустить локальный PHP-сервер.

Откройте терминал в корневой папке проекта и выполните команду:

```bash
php -S localhost:8000